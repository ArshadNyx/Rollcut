node_modules/
out/
*.log

# Never commit credentials
.env
.env.*
!.env.example
.DS_Store

# Local Claude Code / tooling state
# CLAUDE.md is the working brief — instructions and business plan, not docs
# for users of this Action.
CLAUDE.md
.claude/settings.local.json
.claude-flow/

# dist/ is intentionally committed: the composite action runs
# `node dist/action.js` straight from the checked-out repo.
